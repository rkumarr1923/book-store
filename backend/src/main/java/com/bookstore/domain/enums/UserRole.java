package com.bookstore.domain.enums;

/**
 * Application role assigned to a registered user.
 * Stored as a STRING in the database column {@code role}.
 */
public enum UserRole {
    CUSTOMER,
    ADMIN
}
