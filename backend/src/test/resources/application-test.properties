# Test-specific Spring configuration
# Overrides the search_vector column definition to VARCHAR for H2 compatibility.
# Also disables enum DDL type generation used by H2 in PostgreSQL mode.
spring.jpa.properties.hibernate.type.preferred_uuid_jdbc_type=CHAR
